farm:
  id: farm_03

images:
  root_directory: /app/data/images

outputs:
  root_directory: /app/data/outputs

upload:
  state_database: /app/data/state/upload_state.db

model:
  registered_name: egg_counter_farm_03
  alias: production
  local_root_directory: /app/models

inference:
  supported_extensions:
    - .jpg
    - .jpeg
    - .png