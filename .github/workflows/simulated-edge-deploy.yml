name: Simulate Edge Deployment

on:
  workflow_dispatch:
    inputs:
      farm:
        description: Farm to deploy
        required: true
        type: choice
        default: farm_01
        options:
          - farm_01
          - farm_02

      image_tag:
        description: Docker image tag
        required: true
        default: latest
        type: string

permissions:
  contents: read
  packages: read

env:
  IMAGE_NAME: ghcr.io/${{ github.repository_owner }}/poultry-edge

jobs:

  deploy:

    name: Simulate Docker deployment

    runs-on: ubuntu-latest

    steps:

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Pull Docker image
        run: |
          docker pull \
            "${IMAGE_NAME}:${{ inputs.image_tag }}"

      - name: Create simulated Edge
        run: |
          mkdir -p \
            "${{ runner.temp }}/poultry-edge/env"

      - name: Configure deployed image
        run: |
          echo "POULTRY_EDGE_IMAGE=${IMAGE_NAME}:${{ inputs.image_tag }}" \
            > "${{ runner.temp }}/poultry-edge/env/poultry-edge.env"

      - name: Show deployed configuration
        run: |
          echo
          echo "Simulated Edge:"
          echo

          cat \
            "${{ runner.temp }}/poultry-edge/env/poultry-edge.env"

      - name: Verify Docker image
        run: |
          docker image inspect \
            "${IMAGE_NAME}:${{ inputs.image_tag }}" \
            --format='ID: {{.Id}}'

      - name: Show simulated Edge structure
        run: |
          echo
          echo "Simulated Edge structure"
          echo

          find \
            "${{ runner.temp }}/poultry-edge" \
            -maxdepth 3 \
            -print

      - name: Prepare deployment evidence
        run: |
          mkdir deployment-evidence

          cp -r \
            "${{ runner.temp }}/poultry-edge" \
            deployment-evidence/

      - name: Upload deployment evidence
        uses: actions/upload-artifact@v4
        with:
          name: edge-deployment-${{ inputs.farm }}
          path: deployment-evidence/